export interface StationModel {
  label: string,
  dist: number,
  iconclass: string
}

export interface StationPoint {
  id: string,
  lat: number,
  lon: number,
  text: string,
  url: string,
  x: string,
  y: string
}
